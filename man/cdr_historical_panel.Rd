% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_historical_panel.R
\name{cdr_historical_panel}
\alias{cdr_historical_panel}
\title{Fetch a Long Historical CDR Indicator Panel}
\usage{
cdr_historical_panel(
  start_year = 2000,
  end_year = NULL,
  codes = NULL,
  cache_dir = tempdir(),
  refresh = FALSE
)
}
\arguments{
\item{start_year}{First year to fetch.  Default \code{2000}.}

\item{end_year}{Last year.  \code{NULL} (default) uses the current year.}

\item{codes}{ISO-2 codes.  \code{NULL} (default) uses the CDR country set.}

\item{cache_dir}{Directory for the cache file.  Default a per-session
temporary directory.}

\item{refresh}{If \code{TRUE}, ignore any cache and re-download.}
}
\value{
A data frame with the same columns as \link{indicators} covering
\code{start_year:end_year}, carrying a \code{sources} attribute.  The cache
file path is in \code{attr(, "cache")}.
}
\description{
Downloads annual GDP, market capitalization, democracy, corruption, and
population for the CDR countries over a multi-year window and caches the
result locally, so the time-invariance analyses can be run on a long
series rather than the few years shipped in \link{indicators}.
}
\examples{
\dontrun{
  hp <- cdr_historical_panel(1995)
  table(hp$year)
}

}
\seealso{
\code{\link[=get_country_indicators]{get_country_indicators()}}, \code{\link[=cdr_update_data]{cdr_update_data()}}
}
