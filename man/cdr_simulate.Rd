% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_simulate.R
\name{cdr_simulate}
\alias{cdr_simulate}
\alias{print.cdr_simulate}
\title{Counterfactual CDR Policy Simulation}
\usage{
cdr_simulate(
  country,
  scenario,
  data = NULL,
  model = NULL,
  n_rep = 1000L,
  conf = 0.95,
  seed = NULL
)

\method{print}{cdr_simulate}(x, ...)
}
\arguments{
\item{country}{ISO-2 code or country name (case-insensitive).}

\item{scenario}{A named numeric vector of additive changes to
standardized variables (\code{C_std}, \code{D_std}, \code{R_std}, \code{N_std}), or a list
of such vectors for multiple scenarios.  Resulting values are clamped
to [0, 1] and \code{CDR} is recomputed as \code{C_std * D_std * R_std}.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}} object, or \code{NULL} to fit one.}

\item{n_rep}{Bootstrap replicates for the CI.  Default \code{1000}.}

\item{conf}{Confidence level.  Default \code{0.95}.}

\item{seed}{Optional integer seed.}

\item{x}{A \code{cdr_simulate} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_simulate"}: a data frame with one row
per scenario (\code{scenario}, \code{baseline_g}, \code{new_g}, \code{delta_g}, \code{lower},
\code{upper}).
}
\description{
Applies one or more changes to a country's standardized CDR variables
and reports the implied change in fitted growth, with a bootstrap
confidence interval from resampling the estimation sample.
}
\examples{
cdr_simulate("US", c(D_std = 0.1), n_rep = 200, seed = 1)

}
