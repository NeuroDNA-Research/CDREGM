% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_reform_path.R
\name{cdr_reform_path}
\alias{cdr_reform_path}
\alias{print.cdr_reform_path}
\title{Minimum-Cost CDR Reform Path to a Growth Target}
\usage{
cdr_reform_path(
  country,
  target_g,
  data = NULL,
  model = NULL,
  cost = c(C_std = 1, D_std = 1, R_std = 1),
  step = 0.02,
  max_iter = 500L
)

\method{print}{cdr_reform_path}(x, ...)
}
\arguments{
\item{country}{ISO-2 code or country name.}

\item{target_g}{Target fitted growth rate.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}}, a coefficient vector, \code{"published"},
or \code{NULL} to fit one.}

\item{cost}{Named numeric vector of relative reform costs for \code{C_std},
\code{D_std}, \code{R_std}.  Default equal costs.}

\item{step}{Maximum change in any single coordinate per iteration.
Default \code{0.02}.}

\item{max_iter}{Iteration cap.  Default \code{500}.}

\item{x}{A \code{cdr_reform_path} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_reform_path"}: a list with \code{path} (a
data frame of \code{iter}, \code{C_std}, \code{D_std}, \code{R_std}, \code{g}), \code{reached}
(logical), \code{iterations}, \code{total_change} (named vector), and
\code{total_cost}.
}
\description{
Starting from a country's current standardized \code{C}, \code{D}, \code{R}, takes
gradient-ascent steps on the fitted growth surface (steepest ascent in
growth per unit reform cost) until predicted growth reaches \code{target_g}
or the frontier is hit.  \code{N} is held fixed.
}
\examples{
p <- cdr_reform_path("Brazil", target_g = 0.08)
p
p$total_change

}
