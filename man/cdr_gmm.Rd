% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_gmm.R
\name{cdr_gmm}
\alias{cdr_gmm}
\alias{print.cdr_gmm}
\title{Dynamic Panel (GMM) CDR Model and Solow Contrast}
\usage{
cdr_gmm(data = NULL, lag_g = 1L)

\method{print}{cdr_gmm}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{lag_g}{Number of lags of \code{g} to include as regressors.  Default
\code{1}.}

\item{x}{A \code{cdr_gmm} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_gmm"}: a list with \code{gmm} (a \code{pgmm}
object or \code{NULL}), \code{cdr_r2} and \code{solow_r2} (adjusted \eqn{R^2} of the
level-CDR and log-linear pooled fits), \code{cdr_fit}, \code{solow_fit}.
}
\description{
Estimates a dynamic CDR growth model with lagged growth by
Arellano-Bond difference GMM (\code{plm::pgmm}), and contrasts the CDR
specification with a Solow-style log-linear one.  The log-linear fit
typically has a much lower \eqn{R^2}: the CDR variables enter through a
multiplicative interaction, not additively in logs.
}
\details{
The bundled \link{indicators} panel spans only a few years, so the GMM
estimator has very little identifying variation and may fail; the
function returns \code{NULL} for \code{gmm} in that case and still reports the
\eqn{R^2} contrast.
}
\examples{
\dontrun{
  m <- cdr_gmm()
  c(cdr = m$cdr_r2, solow = m$solow_r2)
}

}
\references{
Arellano, M. & Bond, S. (1991). Some tests of specification for panel
data. \emph{Review of Economic Studies}, 58(2), 277-297.
}
