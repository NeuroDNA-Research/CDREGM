% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_spatial.R
\name{cdr_spatial}
\alias{cdr_spatial}
\alias{print.cdr_spatial}
\title{Spatial Dependence in CDR Growth}
\usage{
cdr_spatial(data = NULL, weights = c("cdr", "latitude"), k = 5L, n_perm = 999L)

\method{print}{cdr_spatial}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{weights}{\code{"cdr"} (default) or \code{"latitude"}.}

\item{k}{Number of nearest neighbours.  Default \code{5}.}

\item{n_perm}{Permutations for the Moran's I test.  Default \code{999}.}

\item{x}{A \code{cdr_spatial} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_spatial"}: a list with \code{moran_g},
\code{moran_index} (each a list from the internal Moran routine),
\code{slx_2sls} (the spatial-lag 2SLS fit: \code{coef}, \code{se}, \code{t}, with \code{rho}
as the \code{Wg} term), optionally \code{sar} / \code{sem} (spatialreg fits), and
\code{W}.
}
\description{
Tests for and models spillovers in the CDR framework.  Neighbours are
defined either in standardized CDR space (\code{weights = "cdr"}, the
institutional-contagion interpretation) or by absolute latitude
(\code{weights = "latitude"}, a crude geographic proxy — the bundled data has
no longitude).
}
\details{
Reports Moran's I for the growth rate and the CDR index (analytic and
permutation p-values, computed without external packages), and estimates
a spatial-lag model \verb{g = rho * W g + X beta + e} by spatial two-stage
least squares (instruments \verb{W X}, \verb{W^2 X}).  If \strong{spatialreg} is
installed, maximum-likelihood SAR and SEM fits are added.
}
\examples{
m <- cdr_spatial(k = 4, n_perm = 99)
m$moran_g[c("I", "p_perm")]

}
\references{
Anselin, L. (1988). \emph{Spatial Econometrics: Methods and Models}. Kluwer.
}
