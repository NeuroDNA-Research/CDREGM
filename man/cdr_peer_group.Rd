% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_peer_group.R
\name{cdr_peer_group}
\alias{cdr_peer_group}
\alias{print.cdr_peer_group}
\title{Nearest Countries in CDR Space}
\usage{
cdr_peer_group(
  country,
  k = 5,
  data = NULL,
  vars = c("C_std", "D_std", "R_std"),
  metric = c("euclidean", "mahalanobis")
)

\method{print}{cdr_peer_group}(x, ...)
}
\arguments{
\item{country}{ISO-2 code or country name (case-insensitive).}

\item{k}{Number of peers to return.  Default \code{5}.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{vars}{Standardized columns defining the space.  Default
\code{c("C_std", "D_std", "R_std")}.}

\item{metric}{\code{"euclidean"} (default) or \code{"mahalanobis"}.}

\item{x}{A \code{cdr_peer_group} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_peer_group"}: a data frame of the \code{k}
nearest countries with \code{iso2c}, \code{country}, \code{distance}, the \code{vars}
columns, and \code{g}, ordered by distance.  The target country's own row
is in \code{attr(, "target")}.
}
\description{
Finds the \code{k} countries closest to a target country in standardized CDR
space, by Euclidean or Mahalanobis distance.
}
\examples{
cdr_peer_group("US")
cdr_peer_group("Brazil", k = 3, metric = "mahalanobis")

}
