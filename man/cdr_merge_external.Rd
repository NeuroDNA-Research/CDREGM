% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_merge_external.R
\name{cdr_merge_external}
\alias{cdr_merge_external}
\title{Join an External Country Dataset to the CDR Panel}
\usage{
cdr_merge_external(external, key = NULL, name = NULL, data = NULL, year = NULL)
}
\arguments{
\item{external}{A data frame of external country data.}

\item{key}{Name of an ISO-2 column in \code{external}.  Provide this or
\code{name}.}

\item{name}{Name of a country-name column in \code{external} to resolve to
ISO-2 via \code{\link[=cdr_iso_lookup]{cdr_iso_lookup()}}.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{year}{Optional year to which the panel is filtered before the
join.}
}
\value{
The CDR panel (or its \code{year} slice) with the columns of
\code{external} appended.  Rows with no match keep \code{NA} in the new
columns; an attribute \code{"unmatched"} lists external rows that did not
join.
}
\description{
Left-joins arbitrary external country data onto the standardized CDR
panel by ISO-2 code.  The external data can be keyed by an existing
ISO-2 column or by a country-name column, in which case names are
resolved with \code{\link[=cdr_iso_lookup]{cdr_iso_lookup()}}.
}
\examples{
ext <- data.frame(country = c("United States", "Brazil", "Japan"),
                  trade_openness = c(0.25, 0.28, 0.37))
m <- cdr_merge_external(ext, name = "country", year = 2021)
m[m$iso2c \%in\% c("US", "BR", "JP"),
  c("iso2c", "country", "trade_openness")]

}
