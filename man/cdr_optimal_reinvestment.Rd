% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_optimal_reinvestment.R
\name{cdr_optimal_reinvestment}
\alias{cdr_optimal_reinvestment}
\alias{print.cdr_optimal_reinvestment}
\title{Entrepreneurship Elasticity and the Reinvestment Trade-off}
\usage{
cdr_optimal_reinvestment(
  data = NULL,
  coefficients = .cdr_tsls_published(),
  target = 1,
  c_hat = 0.85
)

\method{print}{cdr_optimal_reinvestment}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{coefficients}{Named 2SLS coefficient vector (\code{C_std}, \code{D_std},
\code{R_std}, \code{CDR}).  Defaults to the published second-stage estimates.}

\item{target}{Elasticity threshold for the \code{elastic} flag.  Default \code{1}.}

\item{c_hat}{Representative exogenous entrepreneurial-capital level.
Default \code{0.85}; \code{NULL} uses each country's own \code{C_std}.}

\item{x}{A \code{cdr_optimal_reinvestment} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_optimal_reinvestment"}: a data frame
with \code{iso2c}, \code{country}, \code{elasticity}, and \code{elastic}, sorted by
elasticity descending.
}
\description{
In the CDR reinvestment model (Ridley & Llaugel 2018, sec. 1.5) a
fraction \eqn{f} of growth is ploughed back into capital.  Working
through the algebra, the \emph{elasticity} of growth with respect to
entrepreneurial capital
\deqn{\varepsilon_i = \hat C_i\,(b_C + b_{CDR} D_i R_i) /
       (b_C \hat C_i + b_D D_i + b_R R_i + b_{CDR}\hat C_i D_i R_i)}
does not actually depend on \eqn{f} -- the reinvestment multiplier
cancels.  This function reports \eqn{\varepsilon_i} per country and
flags where growth is elastic (\eqn{\varepsilon_i \ge} \code{target}).  The
paper's calibrated world reinvestment rate of about 0.21 (10\\% base +
a spread over elastic economies + 3.5\\% depreciation) is returned as a
reference in \code{attr(, "world_rate")}.
}
\examples{
head(cdr_optimal_reinvestment())

}
