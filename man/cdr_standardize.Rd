% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_core.R
\name{cdr_standardize}
\alias{cdr_standardize}
\title{Min-Max Standardize a Numeric Vector}
\usage{
cdr_standardize(x, lo = NULL, hi = NULL)
}
\arguments{
\item{x}{Numeric vector.}

\item{lo}{Lower bound. Defaults to \code{min(x, na.rm = TRUE)}.}

\item{hi}{Upper bound. Defaults to \code{max(x, na.rm = TRUE)}.}
}
\value{
Numeric vector of the same length as \code{x}, values in [0, 1].
\code{NA} inputs produce \code{NA} outputs; a degenerate range (\code{hi == lo})
returns a vector of zeros.
}
\description{
Rescales \code{x} to [0, 1] using \code{(x - lo) / (hi - lo)}, clamped to the
closed interval.  Used internally by \code{cdr_build_panel()} and exported so
that callers can standardize external data against known bounds.
}
\examples{
cdr_standardize(c(0, 5, 10))                     # 0.0  0.5  1.0
cdr_standardize(c(2, 4, 6), lo = 0, hi = 10)     # 0.2  0.4  0.6

}
