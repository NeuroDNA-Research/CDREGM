% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_democratic_friction.R
\name{cdr_democratic_friction}
\alias{cdr_democratic_friction}
\alias{print.cdr_democratic_friction}
\title{Quantify the Democratic-Friction Term}
\usage{
cdr_democratic_friction(data = NULL, model = NULL)

\method{print}{cdr_democratic_friction}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}}, a named coefficient vector, the string
\code{"published"} (the CDRN OLS estimates from Ridley & Khan 2018), or
\code{NULL} to fit one.}

\item{x}{A \code{cdr_democratic_friction} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_democratic_friction"}: a data frame
with \code{iso2c}, \code{country}, \code{cdr} (the interaction value),
\code{friction_growth} (\code{b_CDR * cdr}, negative), \code{friction_usd_pc}
(\verb{friction_growth * GDP per capita}), \code{d_contribution},
\code{r_contribution}, and \code{net_institutional} (\code{d_contribution + r_contribution + friction_growth}), sorted by the magnitude of
\code{friction_growth}.
}
\description{
The CDR model carries a negative coefficient on the \code{C * D * R}
interaction: the cost of decision-making disagreement in a democracy.
This function expresses that term per country as a growth-rate cost, a
dollar-per-capita cost, and alongside the positive contributions of
democracy and rule of law, so the net institutional effect is visible
(the negative interaction is not an "anti-democracy" result).
}
\examples{
head(cdr_democratic_friction())

}
\references{
Ridley, D. & Khan, A. (2018). A new CDR index and its implications for
entrepreneurship and economic growth (democratic friction).
}
