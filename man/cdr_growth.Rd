% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_models.R
\name{cdr_growth}
\alias{cdr_growth}
\title{Estimate a CDR Economic Growth Model}
\usage{
cdr_growth(data, gdp_growth, cdr_vars, controls = NULL, weights = NULL)
}
\arguments{
\item{data}{A \code{data.frame} containing the variables listed below.}

\item{gdp_growth}{Character. Name of the dependent variable column
(annualised GDP per-capita growth rate).}

\item{cdr_vars}{Character vector. Names of the CDR institutional
variables (e.g. \code{c("prop_rights", "democracy_index", "rule_of_law")}).}

\item{controls}{Character vector. Names of additional control variables
(e.g. initial income, investment rate, human capital).  Defaults to
\code{NULL} (no extra controls).}

\item{weights}{Optional numeric vector of observation weights passed to
\code{\link[stats:lm]{stats::lm()}}.}
}
\value{
An object of class \code{"cdr_model"} (a list) with components:
\describe{
\item{fit}{The underlying \code{lm} object.}
\item{formula}{The formula used.}
\item{cdr_vars}{Character vector of CDR variable names.}
}
}
\description{
Fits an OLS growth regression that includes indicators for capitalism
(property rights / economic freedom), democracy (political rights), and
rule of law as explanatory variables alongside standard Solow-type controls.
}
\examples{
set.seed(42)
n <- 80
df <- data.frame(
  growth      = rnorm(n, 2, 1.5),
  prop_rights = runif(n, 0, 10),
  democracy   = runif(n, 0, 10),
  rule_of_law = runif(n, 0, 10),
  ln_gdp0     = rnorm(n, 8, 1)
)
m <- cdr_growth(df,
                gdp_growth = "growth",
                cdr_vars   = c("prop_rights", "democracy", "rule_of_law"),
                controls   = "ln_gdp0")
summary(m$fit)

}
\references{
Acemoglu, D., Johnson, S., & Robinson, J. A. (2001).
The colonial origins of comparative development.
\emph{American Economic Review}, 91(5), 1369–1401.
}
