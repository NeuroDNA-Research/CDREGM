% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_add_country.R
\name{cdr_add_country}
\alias{cdr_add_country}
\title{Add a Country to the CDR Panel}
\usage{
cdr_add_country(
  code,
  indicators_row,
  country_row = NULL,
  indicators = CDREGM::indicators,
  countries = CDREGM::countries
)
}
\arguments{
\item{code}{ISO-2 code of the new country.}

\item{indicators_row}{A data frame of indicator rows for the new
country with columns \code{iso2c}, \code{year}, \code{gdp}, \code{capitalization},
\code{democracy}, \code{corruption}, \code{population}.}

\item{country_row}{A one-row data frame with \code{code}, \code{country},
\code{latitude}, \code{natural_resources} (and optionally
\code{natural_resources_year}).  Required only when \code{code} is not already
in \link{countries}.}

\item{indicators, countries}{Base datasets to extend.  Default the
bundled \link{indicators} and \link{countries}.}
}
\value{
A list with \code{indicators}, \code{countries} (the extended data
frames), \code{panel} (the rebuilt \code{\link[=cdr_build_panel]{cdr_build_panel()}} output), and
\code{extrapolation} (named logical: which raw inputs are outside the
original sample range).
}
\description{
Appends a new country's indicator observations (and, if it is not
already in \link{countries}, its latitude and natural-resource metadata) to
the bundled data, rebuilds the standardized panel, and warns when any
of the new country's inputs fall outside the range of the original
sample -- standardized scores for that country are then extrapolations.
}
\examples{
new_rows <- data.frame(
  iso2c = "SG", year = 2021:2022,
  gdp = c(4.5e11, 4.9e11), capitalization = c(180, 175),
  democracy = c(0.41, 0.42), corruption = c(2.1, 2.1),
  population = c(5.45e6, 5.64e6))
out <- cdr_add_country("SG", new_rows,
  country_row = data.frame(code = "SG", country = "Singapore",
                           latitude = 1.35, natural_resources = 0))
out$extrapolation

}
