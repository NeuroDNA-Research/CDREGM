% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_update_data.R
\name{cdr_update_data}
\alias{cdr_update_data}
\title{Refresh the Bundled CDR Datasets}
\usage{
cdr_update_data(codes = NULL, start_year = NULL, end_year = NULL, path = NULL)
}
\arguments{
\item{codes}{ISO-2 codes.  \code{NULL} (default) uses the CDR country set.}

\item{start_year, end_year}{Year window for the indicator panel.  \code{NULL}
uses \code{\link[=get_country_indicators]{get_country_indicators()}}'s default (the last five years).}

\item{path}{If given, a directory into which \code{indicators.rda} and
\code{countries.rda} are written (xz-compressed).}
}
\value{
A list with \code{indicators} and \code{countries} data frames.
}
\description{
Re-downloads the indicator panel (World Bank WDI/WGI and V-Dem) and the
natural-resource-rent series, and reassembles the \link{indicators} and
\link{countries} data frames.  Latitudes are static and carried over from the
current \link{countries} dataset.  Intended for package maintenance -- run
it, inspect the result, then save it into \verb{data-raw/}.
}
\examples{
\dontrun{
  fresh <- cdr_update_data()
  str(fresh$indicators)
}

}
\seealso{
\code{\link[=get_country_indicators]{get_country_indicators()}}, \code{\link[=cdr_historical_panel]{cdr_historical_panel()}}
}
