% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_growth_accounting.R
\name{cdr_growth_accounting}
\alias{cdr_growth_accounting}
\alias{print.cdr_growth_accounting}
\title{Shapley Decomposition of a Country's Growth Change}
\usage{
cdr_growth_accounting(country, from_year, to_year, data = NULL, model = NULL)

\method{print}{cdr_growth_accounting}(x, ...)
}
\arguments{
\item{country}{ISO-2 code or country name.}

\item{from_year, to_year}{The two years to compare.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}}, \code{"published"}, or \code{NULL}.}

\item{x}{A \code{cdr_growth_accounting} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_growth_accounting"}: a list with
\code{contributions} (named vector for \code{C}, \code{D}, \code{R}, \code{N} that sums to
\code{total}), \code{total}, \code{g_from}, \code{g_to}, and the two years.
}
\description{
Splits the change in fitted growth for one country between two years
into additive contributions from the moves in \code{C}, \code{D}, \code{R}, and \code{N}.
Because the model contains the \code{C * D * R} interaction the split is not
unique by ordering, so Shapley values (the average marginal
contribution over all 24 orderings) are used; they sum exactly to the
total change.
}
\examples{
\dontrun{
  cdr_growth_accounting("Poland", 2022, 2024)
}

}
