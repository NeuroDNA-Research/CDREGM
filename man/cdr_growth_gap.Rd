% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_growth_gap.R
\name{cdr_growth_gap}
\alias{cdr_growth_gap}
\alias{print.cdr_growth_gap}
\title{Growth Gap From the CDR Frontier}
\usage{
cdr_growth_gap(data = NULL, model = NULL, frontier = 1)

\method{print}{cdr_growth_gap}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}} object, or \code{NULL} to fit one.}

\item{frontier}{Target standardized level for C, D and R.  Default \code{1}.}

\item{x}{A \code{cdr_growth_gap} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_growth_gap"}: a data frame with
\code{iso2c}, \code{country}, \code{g_current}, \code{g_frontier}, \code{gap_total}, \code{gain_C},
\code{gain_D}, \code{gain_R}, \code{priority} (variable with the largest single gain),
sorted by \code{gap_total} descending.
}
\description{
For each country, computes the gap between its predicted growth and the
growth it would attain on the CDR frontier (\code{C_std = D_std = R_std = frontier}), and attributes that gap to the individual reforms.  Growth is
the fitted \code{\link[=cdr_ols]{cdr_ols()}} model evaluated with the interaction rebuilt as
\code{C_std * D_std * R_std}, so \code{g_current} may differ slightly from the
model's stored fitted value (which uses the period-averaged \code{CDR}).  Each
\verb{gain_*} is the change in fitted growth from raising that one variable
to the frontier with the others held at current values; because the
model contains the \code{C*D*R} interaction the single-variable gains do not
sum exactly to \code{gap_total}.
}
\examples{
head(cdr_growth_gap())

}
