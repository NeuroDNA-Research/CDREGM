% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_structural_break.R
\name{cdr_structural_break}
\alias{cdr_structural_break}
\alias{print.cdr_structural_break}
\title{Test the CDR Coefficients for Structural Breaks Over Time}
\usage{
cdr_structural_break(data = NULL, breakpoint = NULL)

\method{print}{cdr_structural_break}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{breakpoint}{Year at which to split for the Chow test.  \code{NULL}
(default) uses the median available year.}

\item{x}{A \code{cdr_structural_break} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_structural_break"}: a list with
\code{joint} (an \code{anova} comparing the pooled and year-interacted models),
\code{chow} (list: \code{F}, \code{df1}, \code{df2}, \code{p.value}, \code{breakpoint}),
\code{by_year} (per-year coefficient data frame), and optionally
\code{bai_perron}.
}
\description{
Two complementary tests of the framework's time-invariance claim:
a joint F-test that the CDR coefficients are equal across all years
(an interaction model), and a Chow test at a single \code{breakpoint} year.
If \strong{strucchange} is installed, a Bai-Perron breakpoint search on the
year-mean growth series is added.
}
\details{
The bundled panel has only a handful of years, so these tests are
illustrative rather than definitive.
}
\examples{
cdr_structural_break()

}
\references{
Bai, J. & Perron, P. (1998). Estimating and testing linear models with
multiple structural changes. \emph{Econometrica}, 66(1), 47-78.
}
