% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_elasticity.R
\name{cdr_elasticity}
\alias{cdr_elasticity}
\title{Entrepreneurship-Capital Elasticity of Growth}
\usage{
cdr_elasticity(
  data = NULL,
  f = 0,
  coefficients = c(C_std = 1.295617, D_std = 0.116963, R_std = 0.275395, CDR = -0.98133),
  c_hat = NULL
)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{f}{Reinvestment fraction, scalar in [0, 1).  Default \code{0}.}

\item{coefficients}{Named numeric vector with \code{C_std}, \code{D_std}, \code{R_std},
\code{CDR}.  Defaults to the published 2SLS second-stage estimates.}

\item{c_hat}{Optional per-country exogenous capital estimate.  \code{NULL}
(default) uses \code{C_std} from the panel.}
}
\value{
A data frame, one row per country, with columns \code{iso2c},
\code{country}, \code{D_std}, \code{R_std}, \code{c_hat}, \code{g_hat}, \code{marginal_return},
\code{elasticity}, sorted by \code{elasticity} descending.
}
\description{
Computes the point elasticity of growth \eqn{g} with respect to
exogenous entrepreneurial capital \eqn{\hat C} for each country, from the
2SLS structural coefficients (Ridley & Llaugel 2018, sec. 1.5).  A
fraction \code{f} of growth is assumed reinvested in capital stock, so
\deqn{\hat g_i = \frac{b_C \hat C_i + b_D D_i + b_R R_i
       + b_{CDR}\hat C_i D_i R_i}{1 - b_C f + (-b_{CDR}) f D_i R_i},}
\deqn{\frac{\partial E[\hat g_i]}{\partial \hat C_i}
       = \frac{b_C + b_{CDR} D_i R_i}
              {1 - b_C f + (-b_{CDR}) f D_i R_i},}
and the elasticity is \eqn{(\hat C_i / \hat g_i)\,
\partial E[\hat g_i]/\partial \hat C_i}.
}
\details{
Note that the reinvestment multiplier cancels in the elasticity
expression, so the reported \code{elasticity} does not vary with \code{f}; only
\code{g_hat} and \code{marginal_return} do.  \code{f} is retained for those two
quantities and for compatibility with the published derivation.
}
\examples{
head(cdr_elasticity(f = 0.21))

}
\references{
Ridley, D. & Llaugel, F. (2018). Advances in the CDR economic theory of
entrepreneurship and GDP.
}
