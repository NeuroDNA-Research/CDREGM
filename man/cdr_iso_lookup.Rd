% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_iso_lookup.R
\name{cdr_iso_lookup}
\alias{cdr_iso_lookup}
\title{Resolve Country Identifiers to ISO-2 Codes}
\usage{
cdr_iso_lookup(x, origin = NULL)
}
\arguments{
\item{x}{Character or numeric vector of country identifiers.}

\item{origin}{Optional \code{countrycode} origin code (e.g. \code{"iso3c"},
\code{"country.name"}, \code{"un"}).  \code{NULL} (default) auto-detects: 2-letter
strings are treated as ISO-2, 3-letter as ISO-3, numeric as UN
numeric, anything else as a country name.}
}
\value{
A character vector of ISO-2 codes the same length as \code{x}, with
\code{NA} for unmatched entries (a warning lists them).
}
\description{
Converts a vector of country identifiers in almost any format --
ISO-2, ISO-3, UN numeric, or a country name in any language -- to
canonical ISO 3166-1 alpha-2 codes.  Wraps the \strong{countrycode} package.
}
\examples{
cdr_iso_lookup(c("USA", "Deutschland", "840", "BR"))

}
