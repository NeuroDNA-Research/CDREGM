% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_panel_model.R
\name{cdr_panel}
\alias{cdr_panel}
\alias{print.cdr_panel}
\title{Fixed- and Random-Effects Panel Estimation of the CDR Model}
\usage{
cdr_panel(data = NULL, index = c("iso2c", "year"))

\method{print}{cdr_panel}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{index}{Length-2 character vector naming the individual and time
columns.  Default \code{c("iso2c", "year")}.}

\item{x}{A \code{cdr_panel} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_panel"}: a list with \code{within},
\code{random} (both \code{plm} objects), \code{hausman} (an \code{htest}), \code{preferred}
(\code{"within"} or \code{"random"}), and \code{coef_table} (robust SEs for the
preferred model).
}
\description{
Estimates the CDR growth model on the full country-year panel (rather
than a single cross-section) with the \strong{plm} package: a one-way
fixed-effects (within) model and a random-effects model, a Hausman test
between them, and cluster-robust (by country) standard errors for the
preferred specification.
}
\details{
Note: the bundled \link{indicators} panel spans only a few recent years, so
within-country variation in the slow-moving institutional variables is
limited and the fixed-effects estimates are correspondingly imprecise.
}
\examples{
\dontrun{
  m <- cdr_panel()
  m$hausman
  m$coef_table
}

}
\references{
Croissant, Y. & Millo, G. (2008). Panel data econometrics in R: the plm
package. \emph{Journal of Statistical Software}, 27(2).
}
