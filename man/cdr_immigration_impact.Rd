% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_immigration_impact.R
\name{cdr_immigration_impact}
\alias{cdr_immigration_impact}
\alias{print.cdr_immigration_impact}
\title{GDP Impact of Migration Between Two Countries}
\usage{
cdr_immigration_impact(
  origin,
  destination,
  n,
  data = NULL,
  year = NULL,
  capital = 1
)

\method{print}{cdr_immigration_impact}(x, ...)
}
\arguments{
\item{origin, destination}{ISO-2 codes or country names.}

\item{n}{Number of migrants.}

\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{year}{Year whose per-capita GDP is used.  \code{NULL} uses the latest
available.}

\item{capital}{Fraction of the destination per-capita output the
migrant reaches immediately.  Default \code{1}.}

\item{x}{A \code{cdr_immigration_impact} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_immigration_impact"}: a list with
\code{destination_gain}, \code{origin_loss}, \code{world_gain} (all in current USD),
plus the per-capita figures used.
}
\description{
A first-order estimate of the effect of moving \code{n} people from an
origin country to a destination country, under the CDR premise that an
immigrant brings their entrepreneurial human capital and, operating in
the destination's democracy and rule-of-law environment, produces at
the destination's per-capita rate (Ridley & Llaugel 2018, sec. 1.6).
}
\details{
The destination gains \code{n * gdp_pc(destination)}, the origin loses
\code{n * gdp_pc(origin)}, and the world gains the difference.  A \code{capital}
fraction can down-weight the immediate destination contribution to
reflect trained-knowledge capital acquired only over time.
}
\examples{
cdr_immigration_impact("India", "United States", n = 1e5)

}
