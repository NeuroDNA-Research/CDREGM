% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_marginal_returns.R
\name{cdr_marginal_returns}
\alias{cdr_marginal_returns}
\alias{print.cdr_marginal_returns}
\title{Marginal Growth Returns to CDR Reforms}
\usage{
cdr_marginal_returns(data = NULL, model = NULL)

\method{print}{cdr_marginal_returns}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{model}{A fitted \code{\link[=cdr_ols]{cdr_ols()}}, \code{"published"}, or \code{NULL}.}

\item{x}{A \code{cdr_marginal_returns} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_marginal_returns"}: a data frame with
\code{iso2c}, \code{country}, \code{dg_dC}, \code{dg_dD}, \code{dg_dR}, and \code{best} (the
variable with the largest marginal return), sorted by the maximum
marginal return descending.
}
\description{
For each country, the partial derivative of fitted growth with respect
to each standardized CDR variable at that country's current values:
\eqn{\partial g/\partial C = \beta_C + \beta_{CDR} D R}, and likewise for
D and R.  These are the "bang per reform unit" the policy tools rank on.
}
\examples{
head(cdr_marginal_returns())

}
