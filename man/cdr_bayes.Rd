% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_bayes.R
\name{cdr_bayes}
\alias{cdr_bayes}
\alias{print.cdr_bayes}
\title{Conjugate Bayesian CDR Regression}
\usage{
cdr_bayes(
  data = NULL,
  prior_mean = "published",
  prior_scale = 0.5,
  prior_shape = 0.001,
  prior_rate = 0.001,
  n_draws = 4000L,
  seed = NULL
)

\method{print}{cdr_bayes}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{prior_mean}{Named coefficient vector for the prior mean, or
\code{"published"} (default) for the CDRN OLS estimates.  Missing design
columns default to a prior mean of 0.}

\item{prior_scale}{Prior standard deviation on each coefficient (the
prior covariance is \code{prior_scale^2 * I}, in units of the residual
variance).  Larger = weaker prior.  Default \code{0.5}.}

\item{prior_shape, prior_rate}{Inverse-Gamma hyperparameters for the
residual variance.  Defaults \code{0.001} (near-flat).}

\item{n_draws}{Posterior draws to return.  Default \code{4000}.}

\item{seed}{Optional integer seed.}

\item{x}{A \code{cdr_bayes} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_bayes"}: a list with \code{posterior} (a
data frame: \code{term}, \code{mean}, \code{sd}, \code{q2.5}, \code{q97.5}), \code{draws} (an
\code{n_draws} x k matrix), \code{sigma} (posterior summary of the residual
SD), and \code{prior}.
}
\description{
Fits the CDR growth model with a Normal-Inverse-Gamma conjugate prior
centred on the published CDR coefficients, giving an analytic posterior
(no MCMC, no compiled dependency).  The prior pulls estimates toward the
published sign structure; \code{prior_scale} sets how strongly.
}
\examples{
b <- cdr_bayes(seed = 1)
b$posterior

}
