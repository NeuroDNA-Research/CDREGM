% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_bootstrap.R
\name{cdr_bootstrap}
\alias{cdr_bootstrap}
\alias{print.cdr_bootstrap}
\title{Non-Parametric Bootstrap Confidence Intervals for the CDR Model}
\usage{
cdr_bootstrap(data = NULL, n_rep = 2000L, conf = 0.95, seed = NULL)

\method{print}{cdr_bootstrap}(x, ...)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{n_rep}{Number of bootstrap replicates.  Default \code{2000}.}

\item{conf}{Confidence level.  Default \code{0.95}.}

\item{seed}{Optional integer seed for reproducibility.}

\item{x}{A \code{cdr_bootstrap} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"cdr_bootstrap"}: a list with \code{replicates}
(a \code{n_rep} x k matrix), \code{ci} (a data frame with \code{term}, \code{estimate},
\code{lower}, \code{upper}), \code{n_rep}, and \code{conf}.
}
\description{
Resamples countries (rows of the cross-section) with replacement, refits
the CDR OLS model on each replicate, and returns percentile confidence
intervals for every coefficient plus two derived quantities: the mean
fitted growth rate and the mean CDR interaction term.
}
\examples{
b <- cdr_bootstrap(n_rep = 200, seed = 1)
b$ci

}
