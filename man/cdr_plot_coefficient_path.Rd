% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_plots.R
\name{cdr_plot_coefficient_path}
\alias{cdr_plot_coefficient_path}
\title{CDR Coefficient Path Over Time}
\usage{
cdr_plot_coefficient_path(
  data = NULL,
  terms = c("C_std", "D_std", "R_std", "CDR", "N_std"),
  min_n = 20L
)
}
\arguments{
\item{data}{A panel from \code{\link[=cdr_build_panel]{cdr_build_panel()}}, or \code{NULL} to build it.}

\item{terms}{Coefficients to display.  Default all five model terms.}

\item{min_n}{Minimum complete observations required to fit a given year.
Default \code{20}.}
}
\value{
A \code{ggplot} object.  The underlying estimates are attached as
\code{attr(, "estimates")}.
}
\description{
Fits the CDR OLS model separately for each available year and plots each
coefficient with a 95\% confidence interval, giving a visual check of the
framework's time-invariance claim.  Requires \strong{ggplot2}.
}
\details{
The bundled \link{indicators} panel covers only a few recent years, so the
path has correspondingly few points; the function works with whatever
years contain a computable growth rate.
}
\examples{
\dontrun{
  cdr_plot_coefficient_path()
}

}
