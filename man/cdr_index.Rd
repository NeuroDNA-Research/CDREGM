% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr_core.R
\name{cdr_index}
\alias{cdr_index}
\title{Compute the CDR Composite Index}
\usage{
cdr_index(data = NULL, year = NULL)
}
\arguments{
\item{data}{A data frame from \code{cdr_build_panel()}, or \code{NULL} to call it
automatically.}

\item{year}{Integer. Filter to this year before computing the index.
Overridden by a non-\code{NULL} \code{data} argument unless \code{year} is also used
to subset that data.}
}
\value{
A data frame with one row per country, sorted descending by
\code{CDRp}, with columns \code{iso2c}, \code{country}, \code{year}, \code{C_std}, \code{D_std},
\code{R_std}, \code{CDRs}, \code{CDRp}.
}
\description{
Produces two index variants per country:
}
\details{
\itemize{
\item \strong{CDRs} (additive) – unweighted mean of C_std, D_std, R_std.
\item \strong{CDRp} (friction-adjusted) – CDRs minus the democratic-friction
interaction C·D·R, then re-standardized to [0, 1].
}

When no \code{year} is supplied the function uses the most recent observation
available for each country.
}
\examples{
idx <- cdr_index()
head(idx)

}
